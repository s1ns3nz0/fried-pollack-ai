# ─────────────────────────────────────────────────────────────────────────
# 중요: pip 설치만으로 "전부 동작"하지 않는다. pip는 파이썬 라이브러리만 깐다.
#       실 레인지 동작은 pip이 아니라 '외부 인프라'(SITL/Gazebo/방화벽/SOC)가 좌우한다.
# ─────────────────────────────────────────────────────────────────────────

# Tier 0 — 기본 실행 (run.py, demo.py)
#   ★ 기본 러너 = LangGraph(interrupt 기반 HITL + 체크포인트). 미설치 시 동일 라우팅의
#     순수 stdlib 러너로 자동 강등되므로, langgraph 없이도 데모는 그대로 돈다.
langgraph>=0.2          # 기본 러너. 없으면 stdlib 폴백(interrupt HITL만 미사용)
pyyaml>=6.0             # 프로파일 편집 반영(없으면 내장 기본값 폴백)
pydantic-settings>=2.0  # 중앙 설정(SecretStr 마스킹). 없으면 stdlib 폴백(redteam_core/settings.py)

# Tier 0b — MCP ToolServer wrapper (AKS+kagent 배포 표면)
mcp>=1.0                # FastMCP HTTP/SSE 서버. CLI 실행에는 필요 없고 mcp_server.py에서만 사용

# 개발 — 테스트/평가 하네스
pytest>=7.0             # tests/ + benchmarks 회귀 게이트

# Tier 1 — 실 MAVLink 트래픽 (range_mode: sitl|hil|live). ★ 이것만으론 부족(아래 인프라 참조)
pymavlink>=2.4          # MAVLink 클라이언트. '대화 상대(라이브 SITL)'가 떠 있어야 실제 연결됨


# ─────────────────────────────────────────────────────────────────────────
# pip으로 설치 불가 — 별도 시스템 인프라 (실 폐루프에 필요)
#   • ArduPilot SITL         : 실 MAVLink 엔드포인트(5760/5790). Gazebo 없이 단독 가능
#   • Gazebo (gz/ign CLI)    : backend=gazebo 물리 root-of-trust 쓸 때만. 시스템 바이너리
#   • nftables/iptables + root: --apply-egress 실 방화벽 설치 시
#   • SOC + Sentinel 워크스페이스: ③ 브릿지 UAV*_CL 적재 대상(SOC 구축 가정)
#   • (참고) redteam-lab/dvd : SITL+Gazebo 컨테이너 번들 — range_mode: sitl 대상으로 사용 가능
# ─────────────────────────────────────────────────────────────────────────
